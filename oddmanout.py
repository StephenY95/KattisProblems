testcases = int(input())

for testcase in testcases:
    individuals = int(input())
    for individual in individuals:
        inviteCode = str(input())
        inviteCodeUsers = [int(invite) for invite in inviteCode]
        output = "Case #" +
        for user in inviteCodeUsers:
            if user == user + 1:
